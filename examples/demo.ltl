// examples/demo.ltl — runnable demo for ltl_dockerfile_parse

import std::io
use ltl_dockerfile_parse as lib

fn main() {
    io::println("ltl_dockerfile_parse demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}